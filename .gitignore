# local database and generated artifacts
data/
reports/
*.db
*.db-wal
*.db-shm

# platform exports — real device data, never commit
Sample data/*
!Sample data/.gitkeep
*.xlsx
!tests/fixtures/*.xlsx

# shared snapshot bundles — every device's IMEI, VIN and ICCID, in one file
*.otabundle

# secrets
.env
# Local deployment settings: password verifiers and the session signing key.
deploy/windows/local-env.ps1
data/secret.key

# build output — 43 MB of binaries, rebuildable with `python build.py`
build/
dist/

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# A data folder rescued from dist during a build; put back when the build finishes.
.build-preserved-data/
